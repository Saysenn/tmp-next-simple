# Deployment Steps

1. Check `.env` — confirm `CONTACT_EMAIL` and `RESEND_FROM_EMAIL` are set correctly
2. Add domain on Vercel — Project Settings → Domains → Add
3. Update Hostinger DNS — point nameservers or A/CNAME records to Vercel
4. Test — submit the contact form and confirm email is received
